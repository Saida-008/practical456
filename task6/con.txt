I used grid() to align labels and entry fields clearly in rows and columns. When the Submit button is clicked, the program reads all inputs from the Entry widgets. It checks that Name and Grade are not empty and Age is a number. If any input is invalid, an error message is displayed using messagebox. Valid input is shown in a formatted label, and the Clear button resets the form for new entries.